class LegendItem {
  constructor(title, color, isFor) {
    this.title = title;
    this.color = color;
    this.isFor = isFor;
    this.noOfCountries = 0;
    this.totalNoOfCountries = 0;
  }
}

export default LegendItem;